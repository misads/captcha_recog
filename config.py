IMAGE_HEIGHT = 24       # be mutiple of 4
IMAGE_WIDTH = 64        # be mutiple of 4
CAPTCHA_LENGTH = 4      # only work on capcha with fixed-length
CHAR_SET_LEN = 36       # 10 digits and 26 lower-case letters
DATASET_SIZE = 3000     # how many images in your train set
BATCH_SIZE = 64         # how many samples to train within a batch
TRAINING_STEPS = 5000   # how many steps to train before saving the model and exiting
